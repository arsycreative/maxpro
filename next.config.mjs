/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "via.placeholder.com",
      "images.unsplash.com",
      "imgsrv2.voi.id",
      "custom-images.strikinglycdn.com",
      "theoneupgroup.com",
    ],
  },
};

export default nextConfig;
